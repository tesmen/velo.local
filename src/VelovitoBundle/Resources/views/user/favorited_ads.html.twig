{% extends 'VelovitoBundle::base.html.twig' %}
{% import '@Velovito/templates/macroses.html.twig' as macro %}
{% block body %}
    <div class="col-md-9">
        <div class="section-title" style="display: block;">
            Избранное (в разработке)
        </div>

        {% for ad in favoritesAds %}
            {{ macroses.advert() }}
        {% endfor %}
    </div>

{% endblock %}