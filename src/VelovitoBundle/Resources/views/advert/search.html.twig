{% extends '@Velovito/layout_blank.html.twig' %}
{% import '@Velovito/templates/macroses.html.twig' as macro %}

{% block body %}
    <div class="row">
        <div class="col-md-3">
            <div class="island island-default">
                Пока только по названию
            </div>
        </div>

        <div class="col-md-9">
            {% for ad in ads %}
                {{ macro.advert(ad) }}
            {% endfor %}
        </div>
    </div>
{% endblock %}
