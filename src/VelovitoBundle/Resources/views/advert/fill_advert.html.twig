{% extends 'VelovitoBundle::layout_blank.html.twig' %}
{% import '@Velovito/templates/macroses.html.twig' as macro %}

{% block body %}
    <div class="row">
        {% include '@Velovito/templates/alert.html.twig' %}
        {% set userPhotoName = advert.getPhoto is empty ? null : advert.getPhoto.getFileName() %}


        <div class="col-md-4">
            <h2>{{ advert.getTitle() }}</h2>

            <div>
                <div style="margin-bottom: 20px;">
                    <img src="{{ userPhotoName | getUserPhoto(200,200) }}" alt="{{ advert.title }}"
                         class="img-thumbnail advert-photo">
                </div>
            </div>

            <div>
            </div>
        </div>

        <div class="col-md-8">
            <h3> {{ advert.getPrice() }} {{ advert.getCurrencyName() }}</h3>
            {{ advert.description }}
        </div>
    </div>

    <div class="alert-wrap alert alert-info">
        Объявление опубликовано! Пожалуйста, заполните детали объявления, чтобы другим участникам было
        проще его найти.
    </div>

    <div class="row">
        <div class="col-md-6">
            {{ form_start(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}